# scraper/.env.example
#
# ATENÇÃO: As variáveis de ambiente do scraper estão centralizadas no .env da raiz.
# Este arquivo existe apenas como referência de quais variáveis o módulo consome.
# Configure os valores reais somente no .env da raiz.
#
# Consulte o .env.example na raiz do projeto para a lista completa.
